# tartiflette-asgi

ASGI adapter for Tartiflette.

## Development

Setup for development:

```bash
python -m venv venv
. venv/bin/activate
pip install -r requirements.txt
```

Run tests:

```bash
pytest
```
